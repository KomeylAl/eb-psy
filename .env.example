# Backend API base URL (must end with /)
# Used by Next.js Route Handlers at runtime
BACKEND_API_URL=https://api.ebrazclinic.ir/

# Optional local/dev fallback (also supported)
# NEXT_PUBLIC_BACKEND_API_URL=http://127.0.0.1:8000/
